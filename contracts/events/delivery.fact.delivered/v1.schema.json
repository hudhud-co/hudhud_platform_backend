{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hudhud.platform/contracts/events/delivery.fact.delivered/v1.schema.json",
  "title": "delivery.fact.delivered v1",
  "description": "ADR-0011 D1 — Delivery-owned custody-ending fact.",
  "allOf": [
    { "$ref": "../envelope/v1.schema.json" },
    {
      "type": "object",
      "properties": {
        "event_type": { "const": "delivery.fact.delivered" },
        "event_version": { "const": 1 },
        "producer": { "const": "delivery" },
        "message_kind": { "const": "integration" },
        "aggregate_scope": { "const": "aggregate" },
        "aggregate_type": { "const": "delivery_stop" },
        "aggregate_id": { "type": "string", "format": "uuid" },
        "aggregate_version": { "type": "integer", "minimum": 1 },
        "pii_present": { "const": false },
        "schema_uri": {
          "type": "string",
          "const": "https://hudhud.platform/contracts/events/delivery.fact.delivered/v1.schema.json"
        },
        "payload": { "$ref": "v1.payload.schema.json" }
      },
      "required": [
        "event_type", "event_version", "producer", "message_kind",
        "aggregate_scope", "aggregate_type", "aggregate_id",
        "aggregate_version", "payload"
      ]
    }
  ]
}
