{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hudhud.platform/contracts/events/finance.fact.payout_requested/v1.schema.json",
  "title": "finance.fact.payout_requested v1",
  "description": "PAY-06 \u2014 a merchant asked to be paid, by one of the four methods v6.3 p.31 and p.34 name. The operating procedure per method is an unresolved Open Item (PAY-07), so no fee, timing or reconciliation detail appears here.",
  "allOf": [
    {
      "$ref": "../envelope/v1.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "event_type": {
          "const": "finance.fact.payout_requested"
        },
        "event_version": {
          "const": 1
        },
        "producer": {
          "const": "finance"
        },
        "message_kind": {
          "const": "integration"
        },
        "aggregate_scope": {
          "const": "aggregate"
        },
        "aggregate_type": {
          "const": "payout_request"
        },
        "aggregate_id": {
          "type": "string",
          "format": "uuid"
        },
        "aggregate_version": {
          "type": "integer",
          "minimum": 1
        },
        "pii_present": {
          "const": false
        },
        "schema_uri": {
          "type": "string",
          "const": "https://hudhud.platform/contracts/events/finance.fact.payout_requested/v1.schema.json"
        },
        "payload": {
          "$ref": "v1.payload.schema.json"
        }
      },
      "required": [
        "event_type",
        "event_version",
        "producer",
        "message_kind",
        "aggregate_scope",
        "aggregate_type",
        "aggregate_id",
        "aggregate_version",
        "payload"
      ]
    }
  ]
}
