{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hudhud.platform/contracts/events/finance.fact.cash_limit_breached/v1.schema.json",
  "title": "finance.fact.cash_limit_breached v1",
  "description": "DRV-A06, OPS-04 \u2014 a driver is at or over their cash limit. Consumed by whoever assigns work; it blocks further COD assignment and never a delivery already at a door (ADR-0003 \u2014 physical delivery is irreversible).",
  "allOf": [
    {
      "$ref": "../envelope/v1.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "event_type": {
          "const": "finance.fact.cash_limit_breached"
        },
        "event_version": {
          "const": 1
        },
        "producer": {
          "const": "finance"
        },
        "message_kind": {
          "const": "integration"
        },
        "aggregate_scope": {
          "const": "aggregate"
        },
        "aggregate_type": {
          "const": "driver_cash_account"
        },
        "aggregate_id": {
          "type": "string",
          "format": "uuid"
        },
        "aggregate_version": {
          "type": "integer",
          "minimum": 1
        },
        "pii_present": {
          "const": false
        },
        "schema_uri": {
          "type": "string",
          "const": "https://hudhud.platform/contracts/events/finance.fact.cash_limit_breached/v1.schema.json"
        },
        "payload": {
          "$ref": "v1.payload.schema.json"
        }
      },
      "required": [
        "event_type",
        "event_version",
        "producer",
        "message_kind",
        "aggregate_scope",
        "aggregate_type",
        "aggregate_id",
        "aggregate_version",
        "payload"
      ]
    }
  ]
}
