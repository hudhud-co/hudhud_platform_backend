{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hudhud.platform/contracts/events/hub.fact.drop_off_accepted/v1.schema.json",
  "title": "hub.fact.drop_off_accepted v1",
  "description": "ADR-0011 H1 — Hub-owned custody-starting fact for the customer drop-off path.",
  "allOf": [
    { "$ref": "../envelope/v1.schema.json" },
    {
      "type": "object",
      "properties": {
        "event_type": { "const": "hub.fact.drop_off_accepted" },
        "event_version": { "const": 1 },
        "producer": { "const": "hub" },
        "message_kind": { "const": "integration" },
        "aggregate_scope": { "const": "aggregate" },
        "aggregate_type": { "const": "hub_drop_off" },
        "aggregate_id": { "type": "string", "format": "uuid" },
        "aggregate_version": { "type": "integer", "minimum": 1 },
        "pii_present": { "const": false },
        "schema_uri": {
          "type": "string",
          "const": "https://hudhud.platform/contracts/events/hub.fact.drop_off_accepted/v1.schema.json"
        },
        "payload": { "$ref": "v1.payload.schema.json" }
      },
      "required": [
        "event_type", "event_version", "producer", "message_kind",
        "aggregate_scope", "aggregate_type", "aggregate_id",
        "aggregate_version", "payload"
      ]
    }
  ]
}
