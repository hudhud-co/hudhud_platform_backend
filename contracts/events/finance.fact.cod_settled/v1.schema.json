{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hudhud.platform/contracts/events/finance.fact.cod_settled/v1.schema.json",
  "title": "finance.fact.cod_settled v1",
  "description": "PAY-01 \u2014 the parcel's COD is now settled. v6.3 p.33: an online payment that succeeded, an approved POS card payment, or physical cash that reached the hub cashier. Finance is the only context that may say this, because it is the only one that sees the cash arrive. Amounts are exact minor units with an explicit currency.",
  "allOf": [
    {
      "$ref": "../envelope/v1.schema.json"
    },
    {
      "type": "object",
      "properties": {
        "event_type": {
          "const": "finance.fact.cod_settled"
        },
        "event_version": {
          "const": 1
        },
        "producer": {
          "const": "finance"
        },
        "message_kind": {
          "const": "integration"
        },
        "aggregate_scope": {
          "const": "aggregate"
        },
        "aggregate_type": {
          "const": "cod_collection"
        },
        "aggregate_id": {
          "type": "string",
          "format": "uuid"
        },
        "aggregate_version": {
          "type": "integer",
          "minimum": 1
        },
        "pii_present": {
          "const": false
        },
        "schema_uri": {
          "type": "string",
          "const": "https://hudhud.platform/contracts/events/finance.fact.cod_settled/v1.schema.json"
        },
        "payload": {
          "$ref": "v1.payload.schema.json"
        }
      },
      "required": [
        "event_type",
        "event_version",
        "producer",
        "message_kind",
        "aggregate_scope",
        "aggregate_type",
        "aggregate_id",
        "aggregate_version",
        "payload"
      ]
    }
  ]
}
