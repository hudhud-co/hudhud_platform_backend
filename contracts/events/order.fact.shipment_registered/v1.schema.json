{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hudhud.platform/contracts/events/order.fact.shipment_registered/v1.schema.json",
  "title": "order.fact.shipment_registered v1",
  "description": "ADR-0011 O1 — Ordering-owned registration fact. Ordering never writes custody.",
  "allOf": [
    { "$ref": "../envelope/v1.schema.json" },
    {
      "type": "object",
      "properties": {
        "event_type": { "const": "order.fact.shipment_registered" },
        "event_version": { "const": 1 },
        "producer": { "const": "ordering" },
        "message_kind": { "const": "integration" },
        "aggregate_scope": { "const": "aggregate" },
        "aggregate_type": { "const": "shipment_request" },
        "aggregate_id": { "type": "string", "format": "uuid" },
        "aggregate_version": { "type": "integer", "minimum": 1 },
        "data_classification": { "const": "confidential" },
        "schema_uri": {
          "type": "string",
          "const": "https://hudhud.platform/contracts/events/order.fact.shipment_registered/v1.schema.json"
        },
        "payload": { "$ref": "v1.payload.schema.json" }
      },
      "required": [
        "event_type", "event_version", "producer", "message_kind",
        "aggregate_scope", "aggregate_type", "aggregate_id",
        "aggregate_version", "payload"
      ]
    }
  ]
}
